import { styled, Typography } from "@mui/material";

export const PriceTextWrapper = styled(Typography)`
  color: #dc371e;
  font-feature-settings: "clig" off, "liga" off;
  font-family: Roboto, sans-serif;
  font-size: 20px;
  font-style: normal;
  font-weight: 700;
  line-height: 27px;
`;
