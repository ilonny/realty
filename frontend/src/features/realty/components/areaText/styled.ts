import { styled, Typography } from "@mui/material";

export const AreaTextWrapper = styled(Typography)`
  color: var(--text-secondary, #9e9e9e);
  font-family: Roboto, sans-serif;
  font-size: 14px;
  font-style: normal;
  font-weight: 400;
  line-height: 20px;
  letter-spacing: 0.15px;
`;
